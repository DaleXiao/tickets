# tickets — 电影票根生成器 设计上下文（impeccable.style）

> 设计权威文档。前端所有工作遵循本文件。非谈判项：**无 AI slop**——若有人看一眼说「这是 AI 生成的」，就算失败。
>
> **SPEC-347（T-636, v1.1）修订记录：**
> - ~~SPEC-345 硬约束「复古方向，禁用衬线字体」~~ 已于 2026-08-16 由 Dale 验收反馈 **推翻**（item 6：时间与片名需用衬线字体优雅排印在票根上）。全站排版改为 **serif-led**。
> - 新增 **light/dark 双主题**（icon-forge 模式，右上角 toggle），dark 主题色板见下方「色彩 & 主题」。
> **SPEC-349（T-638, v1.3）修订记录：**
> - UI 中文文案去衬线（Noto Sans SC），票面衬线只保留在**生成图片**里（image prompt 的 Didone/Garamond 指令不动，不经浏览器字体）。
> - 全面禁 emoji（UI/文案/生成图）；loading 齿轮改 icon-forge 同款太阳放射线 spinner。

## 设计上下文

### 用户
任何想为一次观影（约会、朋友重看、通宵场、给票根留个纪念）生成一张纪念票根的人。场景是**轻松、怀旧、随手一玩**——不是严肃工具。用户输入片名 + 场次时间，得到一张「像从票夹里翻出来」的老票根。

### 品牌个性
三个词：**怀旧、温润、利落。**

像一台 1970 年代电影院售票机的吐票口——按键是咔哒的，纸张是带纤维的，油墨是有压痕的。不「酷炫 AI」，不霓虹，不游戏化尖叫。

### 美学方向
**Editorial 复古电影票根（vintage cinema ticket stub / letterpress + 编辑部排版感）。** 参考坐标：

- 老式电影票 / 剧场票——打孔撕边、虚线齿孔、等宽字段、印刷体感。
- 凸版印刷（letterpress）——油墨压进纸里，不是浮在玻璃上。
- impeccable.style 的 editorial 语言：film grain 叠层、编号分节、克制动效、大留白。

**反例（明确不做）：** SaaS 仪表盘卡片墙、青底黑字「AI 感」、紫→蓝渐变、玻璃拟态、霓虹发光、大数字 hero 指标、纯黑纯白、圆角卡片网格、Inter/Roboto/系统默认字体、emoji 图标、居中 slogan 套话。

### 排版（SPEC-349：UI 中文去衬线，票面衬线仅烘焙进生图）
- **西文展示/片名：Cormorant Garamond**（Didone/Garamond 气质的 editorial serif，票面正文的气质来源）。
- **界面中文：Noto Sans SC**（UI 中文一律 sans；生成图片里的票面文字保持衬线，由 image prompt 的 Didone/Garamond 指令控制，不经浏览器字体）。
- **UI chrome / 标签 / 按钮：Space Grotesk**——非默认无性格的 grotesque，用于「表单字段标签」这类功能性文字，不是「mono=技术感」的偷懒。
- 票面字段（日期/时间）在 HTML 回显处用西文衬线 + `tabular-nums` 对齐（中文回显为 sans）。
- 模块化比例 1.25，单一比例，全站一致。展示字号 `clamp()` 流式。

### 色彩 & 主题
**浅色（暖纸）：**

- 背景 `oklch(0.965 0.014 82)` 暖奶油库存纸（绝非纯白）+ film grain 叠层。
- 墨色近黑文字 `oklch(0.26 0.02 55)`（绝非 #000）。
- 单一强调色：**金 `#d4a054`**，只用于「生成」动作 + 品牌点 + focus，绝不摊平铺满。

**深色（深暖底，impeccable 主体默认）：**

- 背景 `#0c0b09`（深暖近黑，非纯黑泛蓝）。
- 抬升面 `#15120d`；墨色文字反转为主 `oklch(0.87 0.014 78)` 暖白。
- 强调色不变：金 `#d4a054`；深色下 hover 提亮为 `#e2b877`。
- 边界线 `oklch(0.28 0.014 72)`，grain 略提高透明度。

现代 CSS：`oklch`、`clamp`。双主题经 `.dark` class + CSS 变量切换，`localStorage` 持久化，index.html 内联脚本首帧防闪烁。

### 布局 & 空间
- 单列居中，大留白。**唯一的「卡片」是票根本身**（它有资格成为票）——输入表单不是「卡片」，是下划线式 editorial 表单 + 编号分节。
- 变化间距（头部/表单/结果/页脚节奏不同），不用千篇一律 padding。
- 票根打孔圆口、虚线撕边、左缘冲孔是**真实票根语汇**，不是装饰。

### 动效
- 克制。一次编排好的加载（生成中骨架 shimmer + 小齿轮转动 + 结果淡入）胜过零散微交互。
- transform/opacity only，指数缓出（ease-out）。尊重 `prefers-reduced-motion`。

### 设计原则
1. **票根语汇即身份**——打孔、撕边、等宽/衬线字段、印刷体感；先把「它像一张票」做对。
2. **克制先于装饰**——每个元素都得有票面的存在理由，拿不准就删。
3. **暖纸 + 墨水 + 一点金**——绝不 AI 霓虹/渐变/玻璃。
4. **serif-led editorial（票面）+ sans UI（界面）**——Cormorant Garamond 撑起西文票面气质，界面中文 Noto Sans SC，Space Grotesk 做 UI chrome；生图内票面衬线由 image prompt 控制。
5. **过 AI-slop 关**——读起来像通用 AI 输出就重做。

## 范围说明
工作树`src/index.css` 是唯一样式入口（有意不用 Tailwind，换取对票根语汇的完全控制）。票根整图由 qwen-image-3.0-pro 直写（文字烘焙进图），前端只做票面框架 + 输入 + 下载/重生成；i18n（`src/i18n.ts`）/ 主题切换与 icon-forge 同模式。