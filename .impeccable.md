# tickets — 电影票根生成器 设计上下文（impeccable.style）

> 设计权威文档。前端所有工作遵循本文件。非谈判项：**无 AI slop**——若有人看一眼说「这是 AI 生成的」，就算失败。
> SPEC-345 硬约束：**复古方向，禁用衬线字体**（grotesque / mono 路线）。

## 设计上下文

### 用户
任何想为一次观影（约会、朋友重看、通宵场、给票根留个纪念）生成一张纪念票根的人。场景是**轻松、怀旧、随手一玩**——不是严肃工具。用户输入片名 + 场次时间，得到一张「像从票夹里翻出来」的老票根。

### 品牌个性
三个词：**怀旧、温润、利落。**

像一台 1970 年代电影院售票机的吐票口——按键是咔哒的，纸张是带纤维的，油墨是有压痕的。不「酷炫 AI」，不霓虹，不游戏化尖叫。

### 美学方向
**复古电影票根（vintage cinema ticket stub / letterpress）。** 参考坐标：

- 老式电影票 / 剧场票——打孔撕边、虚线齿孔、等宽字段、印刷体感。
- 凸版印刷（letterpress）——油墨压进纸里，不是浮在玻璃上。
- 售票机吐出的热敏票——但去掉「收据廉价感」，保留「票面字段适合等宽」这个真实理由。

**反例（明确不做）：** SaaS 仪表盘卡片墙、青底黑字「AI 感」、紫→蓝渐变、玻璃拟态、霓虹发光、大数字 hero 指标、纯黑纯白、圆角卡片网格、Inter/Roboto/系统默认字体。

### 排版（grotesque + mono，禁用衬线）
- **展示/正文：Archivo**（grotesque，可变字重 100–900）。复古印刷感的几何 grotesque，不是 Inter/Roboto 那类「默认无性格」。
- **票面字段/标签：IBM Plex Mono**——这里用 mono 是**有真实理由的**（票面数据字段天然适合等宽：片名行、日期时间的 tabular 对齐），不是「mono=技术感」的偷懒。
- 禁用衬线（SPEC-345 硬约束）。不用 system-default-as-design。
- 模块化比例 1.25，单一比例，全站一致。展示字号 `clamp()` 流式。

### 色彩 & 主题
- **暖纸张背景**（`oklch(0.957 0.013 84)`，奶油库存纸，绝非纯白）+ 极淡纸张颗粒。
- **暖墨近黑文字**（`oklch(0.235 0.016 68)`，绝非 #000）。
- **单一强调色：影院红**（`oklch(0.55 0.19 26)`），只用于「生成」动作 + 品牌点，绝不摊平铺满。
- 现代 CSS：`oklch`、`clamp`。

### 布局 & 空间
- 单列居中，大留白。**唯一的「卡片」是票根本身**（它有资格成为票）——输入表单也不是「卡片」，是带撕边的票根，而非 SaaS card。
- 变化间距（头部/表单/结果/页脚节奏不同），不用千篇一律 padding。
- 票根打孔圆口、虚线撕边、左缘冲孔是**真实票根语汇**，不是装饰。

### 动效
- 克制。一次编排好的加载（生成中骨架 shimmer + 结果淡入）胜过零散微交互。
- transform/opacity only，指数缓出（ease-out）。尊重 `prefers-reduced-motion`。

### 设计原则
1. **票根语汇即身份**——打孔、撕边、等宽字段、印刷体感；先把「它像一张票」做对。
2. **克制先于装饰**——每个元素都得有票面的存在理由，拿不准就删。
3. **暖纸 + 墨水 + 一点红**——绝不 AI 霓虹/渐变/玻璃。
4. **grotesque + mono，禁衬线**——复古的现代转译，不是假古董。
5. **过 AI-slop 关**——读起来像通用 AI 输出就重做。

## 范围说明
工作树`src/index.css` 是唯一样式入口（有意不用 Tailwind，换取对票根语汇的完全控制）。票根整图由 qwen-image-3.0-pro 直写（文字烘焙进图），前端只做票面框架 + 输入 + 下载/重生成。